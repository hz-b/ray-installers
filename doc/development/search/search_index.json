{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"RAY-UI Documentation (development)","text":"<p>Ray tracing for synchrotron beamlines, developed at Helmholtz-Zentrum Berlin.</p> <p>This describes the development build</p> <p>These pages are built from the <code>doc/</code> directory of the RAY-UI source repository, from the branch the nightly builds. They describe what the current development version does, which is not necessarily what a release does. A release publishes its own pages beside these, under <code>\u2026/ray-installers/doc/release/</code>.</p>"},{"location":"index.html#pages","title":"Pages","text":"<ul> <li> <p>Looper preset format \u2014 the JSON a Looper preset is   written in (<code>*.rayloop.json</code>), for the GUI's Load/Save and for the command   line's <code>looper validate|load|run</code>.</p> </li> <li> <p>Field propagation in urad \u2014 how the radiation   field is propagated back from the pinhole to the source plane, why the direct   sum is a discrete Fourier transform, what the FFT form approximates and what   it does not, and how SRW and SPECTRA do the same step.</p> </li> <li> <p>Surface errors and edge diffraction \u2014 when a   mirror profile is better treated wave-optically than by its local slopes,   the Poisson-PSD scattering that does it, Fraunhofer diffraction at mirror   apertures, and how to use both.</p> </li> </ul> <p>More pages follow as they are prepared: the accelerator data file format, trace reproducibility, the radiometric quantities, and the physics notes behind the undulator and dipole sources.</p>"},{"location":"index.html#where-else-to-look","title":"Where else to look","text":"<ul> <li>Command line: <code>rayui -h</code>, or Help \u2192 Command Line Syntax in the   application, lists every option of the background mode.</li> <li>Installers and updates: https://hz-b.github.io/ray-installers/</li> <li>Questions: Help \u2192 Support in the application opens a mail dialogue that   reaches the developers.</li> </ul>"},{"location":"FieldPropagationInUrad.html","title":"Field propagation in urad \u2014 the direct sum and the transform","text":"<p><code>UndulatorSource</code> gets the ray direction from the radiation field at a pinhole some distance downstream, and the ray origin from that field propagated back to the source plane. This note is about that second step: what it computes, how <code>urad_phase_prop_classic</code> computes it, why the same thing can be had from a Fourier transform, and what is and is not approximated on the way.</p> <p>Both propagators live side by side. Which one runs is the Field Propagation setting of the undulator source, saved with the file:</p> setting what runs <code>Fourier transform</code> the transform, and the default <code>direct sum</code> the original <p>Section 9 shows how to run the two against each other.</p>"},{"location":"FieldPropagationInUrad.html#1-what-has-to-be-computed","title":"1. What has to be computed","text":"<p>The pinhole plane sits at a fixed distance <code>dx</code> in front of the source plane, and both are flat. Given the complex field <code>E(y',z')</code> sampled on the pinhole grid, the field on the source grid is the Kirchhoff integral, discretised:</p> <pre><code>A(y,z) = sum_{y',z'} E(y',z') * exp(i*k*DRRED) / DR * da / lambda\n</code></pre> <p>with <code>r = sqrt(dx^2 + (y'-y)^2 + (z'-z)^2)</code> the distance between the two points, <code>DR = r</code>, and <code>DRRED = -(r - dx)</code> the path with the constant <code>dx</code> taken out so that the phase stays small. <code>da</code> is the pinhole area element and <code>k = 2*pi/lambda</code>.</p>"},{"location":"FieldPropagationInUrad.html#2-the-direct-sum","title":"2. The direct sum","text":"<p><code>urad_phase_prop_classic</code> evaluates that literally: for every source point, a loop over every pinhole point. It expands the distance rather than taking a square root each time,</p> <pre><code>eps = ((y'-y)^2 + (z'-z)^2) / dx^2\nans = sqrt(1+eps) - 1, as a Taylor series to sixth order in eps\nDR  = |dx*(ans+1)|,  DRRED = -|dx*ans|\n</code></pre> <p>and refuses to run when <code>eps &gt; 0.01</code>, which is the range in which that series is good. The cost is <code>O(N_source * N_pinhole)</code>. On the QMAP beamline it is about 80 % of a trace, and it grows with the product of the two point counts: a 205 x 205 pinhole grid onto a 256 x 256 source grid takes 100 s.</p>"},{"location":"FieldPropagationInUrad.html#3-why-the-sum-is-a-discrete-fourier-transform","title":"3. Why the sum is a discrete Fourier transform","text":"<p>The two planes are flat and a fixed distance apart, so <code>dx</code> is the same for every pair, and <code>eps</code> depends only on the differences <code>y'-y</code> and <code>z'-z</code>. <code>ans</code> is a function of <code>eps</code> alone, so <code>DR</code> and <code>DRRED</code> are functions of the differences alone. The kernel is a convolution kernel \u2014 at every order of the Taylor series, not just the first. The double loop is a discrete convolution evaluated pair by pair.</p> <p>Keeping the leading term, <code>ans -&gt; eps/2</code>, gives the Fresnel kernel</p> <pre><code>exp(-i*k*((y'-y)^2 + (z'-z)^2) / (2*dx))\n</code></pre> <p>and expanding the square separates the three coordinates:</p> <pre><code>A(y,z) = C * exp(-i*k*(y^2+z^2)/2dx)\n           * sum E(y',z') exp(-i*k*(y'^2+z'^2)/2dx) exp(+i*k*(y*y' + z*z')/dx)\n</code></pre> <p>What is left inside the sum is a discrete Fourier transform: multiply the pinhole field by a quadratic phase, transform, multiply by another quadratic phase. This is the one-step or single-FFT Fresnel propagator, sometimes called propagation to the waist; see Goodman ch. 4 or Voelz ch. 5.</p> <p>The transform pairs a pinhole step <code>dz</code> with a source step</p> <pre><code>dz_source = lambda * dx / (N * dz)\n</code></pre> <p>so the source plane it produces covers exactly <code>lambda*dx/dz</code>, whatever <code>N</code> is.</p>"},{"location":"FieldPropagationInUrad.html#4-is-the-fft-an-approximation","title":"4. Is the FFT an approximation?","text":"<p>Three separate questions hide behind that one, and they have different answers.</p> <p>The FFT itself: no. Cooley\u2013Tukey evaluates the discrete Fourier transform exactly, to rounding error. It is a faster way of computing the same sum, not a different sum.</p> <p>The kernel: yes, and less accurately than the direct sum. The transform keeps only the leading term of the path expansion, where the direct sum keeps six, and it takes the amplitude <code>1/DR</code> as <code>1/|dx|</code> where the direct sum keeps the full <code>1/r</code>. These are the only two approximations the transform adds. Measured against the direct sum on identical points:</p> pinhole grid aperture worst |dA|/max|A| rms |dA|/rms|A| 51 x 51 1.395 mm 2.2e-09 1.1e-08 99 x 71 1.395 mm 2.2e-09 1.2e-08 205 x 51 5 mm 3.9e-06 8.0e-06 205 x 205 5 mm 3.9e-06 8.0e-06 <p>The residual grows with the aperture, as it must: both dropped terms scale with <code>eps</code>. At the apertures the source is actually used with it is at the level of 1e-8 to 1e-5, far below anything the ray tracing is sensitive to.</p> <p>This is also not a fundamental limit. Because the exact kernel is a convolution at all orders (section 3), a same-grid FFT convolution with the untruncated kernel would match the direct sum to machine precision. It is not used here because the two planes have different grid spacings, and the one-step form is what turns that difference into the transform's own output grid.</p> <p>The periodicity: no, and this is the important one. A discrete transform is periodic, so the reconstructed source plane repeats with period <code>lambda*dx/dz</code>. That is not something the FFT introduces \u2014 the direct sum has exactly the same periodicity, because it is the same discrete convolution. The difference is that the direct sum lets the caller ask for a source window wider than one period, and then quietly returns replicas of the source. That failure was measured before the transform existed: at 800 eV with a 5 mm pinhole on 151 points the period is 465 um, and a 2 mm window produced five copies of the source at 463 um spacing, with the ray origins spread over all of them.</p> <p>In the transform the window is not an input at all. It is cut out of one period, so the failure mode cannot be configured.</p>"},{"location":"FieldPropagationInUrad.html#5-both-settings-on-the-same-beamline","title":"5. Both settings on the same beamline","text":"<p>Traced on the QMAP source at 800 eV, 200000 rays, with the pinhole at 10 m on the 141 x 121 points its derivation gives. The core is read from the half width, the total from the rms, because the pedestal of section 4 moves the second and not the first.</p> setting sigma_x core sigma_y core trace Fourier transform 27.1 um 25.3 um 22.3 um 19.6 um 17.0 s direct sum 27.2 um 25.3 um 22.4 um 19.1 um 21.7 s <p><code>sqrt(beta*epsilon + sigma_r^2)</code> with Walker's model 4 predicts 25.7 um by 20.0 um.</p> <p>Without the analysis the transform's trace takes 13 s. Its propagation is below a second, where the direct sum's takes 4 s; the rest is urad computing the field on the pinhole points (about 5 s, 0.3 ms a point), generating the rays (3 s) and tracing the beamline (4.5 s).</p> <p>The transform was once the slower of the two here. The source grid it makes is one replica period wide -- 1.089 mm against the 360 um of the window -- and the rejection samplers draw over whatever window they are given, so what the propagation saved, the sampling gave back. Since the transform cuts its grid down to the source (<code>urad_prop_crop</code>), that is gone. A third setting, \"transform, then direct sum\", ran the transform only to size the direct sum's window; with the crop in place it gave the direct sum's rays in the direct sum's time, and it was removed.</p>"},{"location":"FieldPropagationInUrad.html#6-what-the-transform-gives-up","title":"6. What the transform gives up","text":"<p>The source grid stops being a free parameter: its extent and step follow from the pinhole grid and the wavelength. That is the point \u2014 it is what removes the replicas \u2014 but it means <code>PinHoleWidthSource</code>, <code>PinHoleHeightSource</code> and the two source point counts are no longer honoured, and <code>UndulatorSource</code> reads back the grid the propagation actually produced. The panel hides the four in that case.</p> <p><code>modesphere</code> places the observation points on a sphere instead of a plane. Then <code>dx</code> is not the same for every pair, the kernel is no longer a function of the differences, and none of section 3 applies. That case keeps the direct sum, which is why the transform is a switch and not a replacement.</p>"},{"location":"FieldPropagationInUrad.html#7-how-finely-the-pinhole-has-to-be-sampled","title":"7. How finely the pinhole has to be sampled","text":"<p>Both propagators read the field off the pinhole grid, and that field is not slowly varying. It carries the propagation phase from the source,</p> <pre><code>k*(r - L)  ~  k*y^2/(2L)\n</code></pre> <p>which winds faster the further out the grid reaches. Across the 2.4 x 2.4 mm pinhole at 10 m at 292 eV that is about seventeen full turns from the centre to the edge, and twice that to the corner. Representing it needs more than two samples per turn:</p> <pre><code>step  &lt;  lambda*L / (2*y_max)  =  lambda*L / aperture\n</code></pre> <p>On that geometry the limit is 17.7 um and the 151-point grid gives 16.0 um, so it holds with about ten percent to spare. Measured on the dumped field, the phase advance per cell climbs from 0.57 rad at y = 0.2 mm to 1.4-1.9 rad by y = 0.6-0.8 mm, against the pi that would break it.</p> <p>The trap is that the step is fixed by the aperture and the point count while the limit shrinks as the aperture grows. Widening the pinhole to look at larger angles, without raising the point count in proportion, walks straight into aliasing: at 4.62 mm the limit is 8.7 um and at 7.2 mm it is 5.6 um, while a 151-point grid still delivers 31 um and 48 um. Nothing downstream notices. The field aliases quietly and the back-propagated source grows structure that is not there \u2014 a 7.2 mm pinhole at 451 points put a ripple of about 8 um into the reconstructed source at 308 eV, which is <code>lambda/(2*theta_max)</code> for that aperture and was absent from every properly sampled run.</p> <p><code>UndulatorSource</code> warns on <code>stderr</code> when the condition is violated on either axis and says how many points it would take.</p> <p>The derivation behind the \"auto\" boxes of the pinhole parameters takes the condition into account. It sizes the aperture from the divergence and then asks for the largest point count of three separate conditions: sampling the far-field profile itself, fitting the source window into one replica period, and this one. They do not agree. On BESSY III this is the tightest \u2014 the UE65 at 800 eV asks 35 points for the period and 141 for the phase \u2014 but on BESSY II, whose electron beam makes the window ten times wider, the period sets the count.</p> <p>That was not always so. The derivation originally knew only the replica period, and the grids it produced aliased at every energy tried, by factors 1.0 to 1.6 from 100 eV to 2 keV. The two conditions are easy to confuse: both are sampling limits and both involve <code>lambda*distance/aperture</code>, but one bounds how far the source window may reach before it sees a replica, and the other how coarsely the pinhole may be sampled before the phase aliases.</p> <p>How much one period has to hold was overstated for a while. urad back-propagates the field of a single electron and folds the electron beam in afterwards, on the window, so what repeats is the single-electron field and not the folded source. The derivation asked for three windows per period, and on the BESSY II U49 at 300 eV and 4 m that meant 891 far-field points where 181 gave the same ray origins to the last bit; the copies set in only below 0.6 to 0.7 of the window, at 100, 300 and 900 eV alike. It now asks for one window per period. The transform cuts its own source grid out of one period, so there the period has to hold only the source, ten sigma of it.</p> <p>Note also what refining costs. The direct sum is O(N_pinhole * N_source), so doubling the aperture and keeping the sampling means four times the pinhole points and four times the run time. The transform is the cheaper way to afford a large, well-sampled aperture.</p>"},{"location":"FieldPropagationInUrad.html#8-how-other-codes-do-it","title":"8. How other codes do it","text":"<p>SRW carries all of these forms and picks one per drift (<code>cpp/src/core/sroptdrf.h</code>, <code>LocalPropMode</code>):</p> mode method 0 angular representation \u2014 FFT to angle, multiply by the free-space transfer function, FFT back; output grid equals input grid 1 propagation to waist \u2014 the one-step Fresnel form used here 2 propagation from waist \u2014 the same in reverse 3 propagation without the quadratic phase term <p><code>ChooseLocalPropMode</code> decides between them from the sampling of the wavefront. Mode 3 is SRW's characteristic one: the quadratic phase is factored out and treated analytically, so only the slowly varying residual is carried on the grid and the grid can be far coarser than the phase would otherwise require (<code>PropagateRadiationSimple_AnalytTreatQuadPhaseTerm</code>). SRW also keeps a direct Fresnel quadrature, <code>PropagateRadiationSimple_NumIntFresnel</code>, for the cases the transforms cannot serve \u2014 the same role the direct sum keeps here.</p> <p>SPECTRA computes the source profile from the far field with separable 1D transforms: one FFT per row, then one per column (<code>spectra_src/source_profile.cpp</code>, <code>SourceProfile::f_SpatialProfileSingle</code>). It also folds the electron distribution by multiplying with the analytic Gaussian transform <code>exp(-sigma^2 k^2 / 2)</code> in Fourier space (<code>SpatialConvolutionFFTBase::GetFFTedProfileAt</code>) rather than convolving with a truncated kernel in real space. urad's own real-space fold is cut off at <code>3*sigma</code>: it leaves the outermost <code>3*sigma</code> of its grid empty and, because the truncated kernel is not renormalised, loses the Gaussian's mass beyond it -- <code>erf(3/sqrt 2) = 0.9973</code> per axis, 0.54 % in two. Inside raycore RAY-UI now folds the way SPECTRA does (<code>urad_fold_gauss_fft</code>).</p> <p>The one-step Fresnel propagator is standard and predates all three codes; the references below are the usual ones for the derivation and for the sampling conditions it imposes.</p>"},{"location":"FieldPropagationInUrad.html#9-running-the-comparison","title":"9. Running the comparison","text":"<p>Field Propagation selects the propagator for a saved run. <code>URAD_PROP</code> overrides it, and adds the comparison, which is a developer switch and deliberately not reachable from the interface:</p> <pre><code>URAD_PROP=direct  the direct sum\nURAD_PROP=fft     the transform\nURAD_PROP=check   run both on identical points and report the difference\nunset             whatever Field Propagation says\n</code></pre> <p><code>check</code> runs the transform first, because its output grid follows from the pinhole grid, then points the direct sum at exactly those points. What is compared is the two algorithms rather than two griddings of the problem. It reports the peak amplitude of each and where it sits, the worst and rms differences, and the timing of both. It also reports the phase step between neighbouring samples for the pinhole field alone and for the field times the chirp, weighted by intensity \u2014 the second must stay below <code>pi</code> for the transform to be valid, and on the QMAP geometry it is 0.06 rad against 1.06 rad for the field alone, because the radiation carries the conjugate spherical phase that the chirp cancels.</p>"},{"location":"FieldPropagationInUrad.html#10-the-electron-energy-spread","title":"10. The electron energy spread","text":"<p>urad does model the energy spread: in its ensemble mode every macro-electron radiates with its own <code>gamma = (1+dpp)*gamma0</code>. RAY-UI drives urad with a single electron, though -- <code>nelec = 1</code> and <code>noranone = 1</code> -- which makes urad set <code>ibunch = 0</code> and <code>iemit = 0</code> and leaves <code>dpp</code> at zero. The transverse beam still arrives, through the analytic fold over y and z, but that fold has no counterpart along the photon energy. Varying the relative energy spread from 0 to 0.02 used to leave a trace identical to every digit.</p> <p>The fold does not need an ensemble. An electron with relative deviation delta has its resonance at <code>E1*(1+2*delta)</code>, so at a fixed observed energy E it radiates as the nominal electron does at <code>E*(1-2*delta)</code>. Folding over delta is therefore a Gaussian along the photon energy with</p> <pre><code>sigma_E = 2 * E * sigma_delta\n</code></pre> <p>which is how SPECTRA does it (<code>SpectraSolver::EnergySpreadSigma</code>, applied in <code>SourceProfile::AllocateProfileEconv</code>). SRW's <code>srwl_wfr_emit_prop_multi_e</code> with <code>_me_approx = 1</code> treats the spread numerically in the same spirit, varying gamma itself and drawing it at random. Varying the photon energy instead is the first-order equivalent, and lets urad reuse one trajectory for all energies.</p> <p>With Energy Spread set to folded along photon energy, <code>UndulatorSource</code> asks urad for Energy Spread Nodes photon energies in one call, 9 by default, spaced evenly over +-3 sigma_E with the traced energy in the middle. They have to be evenly spaced: the direct sum advances the phase from one energy to the next recursively. The pinhole and source grids urad returns are summed across the energies with Gaussian weights -- intensities, since electrons of different energy radiate incoherently -- and everything downstream, the flux included, works on the folded grids.</p> <p>Two details make the transform safe for this. The source grid it lays out depends on the wavelength; it is taken from the middle energy, so the slice the fold weights most is exact and the others are off by <code>(E_k-E_mid)/E_mid</code>, a few tenths of a percent. And the crop that trims the transform grid measures the source on the middle slice, not the first -- the lowest energy, whose source above the resonance has a different shape.</p> <p>Measured at 307 eV, above the resonance where the spread matters most, on the UE65 at 10 m. The contrast is the mean intensity within r &lt; 15 um over that between 35 and 50 um:</p> <pre><code>                                  contrast    trace time\nfold off, direct sum                0.39         7.1 s\nfold off, transform                 0.36         1.3 s\nfold on,  direct sum                0.76        24.4 s\nfold on,  transform                 0.75         5.5 s\nfold on,  transform, 13 nodes       0.75         7.6 s\nSPECTRA                             0.80\n</code></pre> <p>The radial profile of the folded transform agrees with SPECTRA's to 0.91-1.02 across the +-50 um window SPECTRA covers; unfolded it was more than a factor two off in the centre. With the fold off the grids are bit-identical to those of the previous build, for both propagators; with the fold on and the spread at zero, identical to the fold off. 9 and 13 nodes agree to 9.4e-4. Both propagators fold the same pinhole grid, bit-identically, and their source planes agree to 3.8 % of the peak.</p> <p>The direct sum costs 3.5 times as much with nine energies rather than nine times, because each further energy only multiplies a pair's phase by a precomputed increment; the transform costs 4.2 times as much and remains the cheaper by a factor four. A folded trace with the transform is faster than an unfolded one with the direct sum.</p> <p>A third setting, \"folded, fast period sum\", gives the same fold for less. In <code>urad_amprep</code> the path, the arrival time and the distance weight of every period depend on the electron and the observation point but not on the photon energy, and the one-period amplitude does not depend on the period. The fast sum walks the periods once per point, keeps the arrival times and weights, and gives every energy a scalar sum of phase factors, stepped from one energy to the next on the evenly spaced grid; the per-energy sum recomputes the geometry and all six field components for every energy. The two agree to rounding, and a selftest holds them to 1e-9 of the peak.</p> <p>Measured, it saves little: on the QMAP source at 800 eV with nine energies, single-threaded, urad takes 9.1 s instead of 9.5 s with the transform and 21.1 s instead of 21.6 s with the direct sum. The period sum was not where the fold's cost lies. That is in the one-period integral, which advances all nine energies at every step of the electron's path, and in the propagation, which runs once per energy.</p> <p>What does save time is fewer nodes, and how few suffice follows from how the spread compares with the width of the line, 2 pi n N delta_rms: 0.44 for the UE65 at the first harmonic, 1.3 at the third. Against 41 nodes, as the largest deviation of the pinhole grid relative to its peak:</p> <pre><code>nodes               first harmonic, 307 eV     third harmonic, 800 eV\nfold off                   16.5 %                     32 %\n3 over +-3 sigma           14.4 %                     31 %\n3, Gauss-Hermite            0.20 %                     4.9 %\n5                           0.12 %                     2.3 %\n7                           0.16 %                     0.17 %\n9                           0.14 %                     0.14 %\n15                          0.08 %                     0.08 %\n</code></pre> <p>Three nodes over +-3 sigma give the outer two a hundredth of the weight each and fold next to nothing in, so three nodes are the Gauss-Hermite rule instead: +-sqrt(3) sigma with weights 1/6, 2/3, 1/6, exact for polynomials to the fifth degree and still an evenly spaced grid. At the first harmonic that is as good as nine; at the third, where the fold lowers the peak by a third, it leaves 5 %, and seven nodes are needed for 0.2 %. The slow decline from seven nodes on is the end nodes at +-3 sigma taking full weight, a first-order error in the node spacing, and stays below 0.2 %. The flux through the pinhole changes by at most 0.04 % from three Gauss-Hermite nodes on, against 2 to 5 % for the fold off.</p> <p>The fold is off by default. Relative Energy Spread defaults to 0.001, so switching the fold on by default would change the result, and with the direct sum the run time, of every existing beamline.</p>"},{"location":"FieldPropagationInUrad.html#11-the-ends-of-the-undulator","title":"11. The ends of the undulator","text":"<p>urad computes the trajectory and the radiation of one period, the one centred on x = 0, and adds it up over N periods: period i enters with the phase the electron has slipped by then and with the distance of that period from the observer (<code>urad_amprep</code>, the loop over <code>nper_u</code>). That is exact for N identical periods of B0 cos(k x) and nothing outside -- a field that switches on at full height and off again. It is also all urad could describe: the field routine <code>bhalba_omp</code> is B0 cosh(k y) cos(k x) everywhere, and the tracking range sets where it starts and stops.</p> <p>Real undulators end in weaker poles that bring the orbit in and out without a kink or an offset, and above the resonance the ends matter. The structure near the axis there scales with 1/N, and a few percent more or less effective periods move the source plane by 10 to 20 %. Measured with SRW and with an independent near-field integral on the UE65 at 10 m, 300 to 310 eV, with the energy spread folded in and the antisymmetric structure of the UE65's SPECTRA input: relative to hard ends, 1/4-3/4 poles change the flux in a +-40 um source window by 0.99-1.02 up to 301 eV and by 0.88-1.16 above, and the smooth termination SRW uses by 0.99-1.02 and 0.95-1.20 (<code>srw/scripts/qmap_source_three_way.py</code> and <code>qmap_exact_near_field.py</code> in the QMAP project).</p> <p>Undulator Ends chooses the end model:</p> <ul> <li>hard -- as urad always had it. The default; the result is unchanged.</li> <li>1/4-3/4 end poles -- full height up to the last zero crossing inside N   periods, then 3/4 of it for half a period and 1/4 for the next: the   \"1/4, -3/4, 1, -1, ...\" end structure, the simplest that zeroes the first   field integral and makes the electron oscillate about the axis (Clarke,   section 7.5.1). SPECTRA builds the same for \"End Correction Magnet\".</li> <li>soft (as SRW) -- full height up to the zero crossing a quarter period   outside N periods, then the continuation   (2 pi d/l)(1 - 4 pi^2 d^2/(9 l^2)) exp(-2 pi^2 d^2/(3 l^2)) of the distance   d past it, with which SRW's <code>SRWLMagFldU</code> terminates an undulator   (<code>cpp/src/core/srmagfld.h</code>, <code>srTMagFieldPeriodic::compB</code>). No publication   describes it; it is cut 1.5 periods past the zero crossing, where it has   fallen below 3e-5.</li> </ul> <p>Since urad's core is a cosine, both are the symmetric variants.</p> <p>The ends are not periodic, so they cannot go into the period sum. They are tracked apart from it instead, as an entrance and an exit section, each once per observation point (<code>urad_end_setup</code>, <code>urad_end_profile</code>, <code>urad_end_field</code>). With 1/4-3/4 poles the core shrinks to N-2 periods and the outermost period on each side joins its end section, which carries the 3/4 pole into it; soft ends keep all N periods in the core. The orbit on the core is urad's periodic one, z = z_amp cos(k x); integrating z'' = -z_amp k^2 p(x) back from the start of the core gives where and at which angle the electron has to enter, and the slippage it builds up on the way, which places the phase of the entrance section before the core. The exit section starts on the periodic orbit and takes the phase the period sum has reached. Each trace prints once how closely the tracked entrance meets the core and at which angle the electron leaves -- the check that the two parts join.</p> <p>The price is the tracking: a section is 1.75 periods long, so a point costs about 4.5 periods of trajectory instead of one. The ends are therefore off by default.</p> <p>Checked on the UE65 at 10 m with the transform:</p> <ul> <li>With hard ends every grid -- pinhole, source plane and field, with and   without the energy spread -- is bit-identical to the previous build.</li> <li>The tracked entrance meets the core within 0.002 um and 0.1 urad, and the   electron leaves the undulator at 0.02 urad (1/4-3/4) and -0.004 urad   (soft), against an oscillation amplitude K/gamma of 410 urad.</li> <li>Against the exact near-field integral of the same field   (<code>qmap_exact_near_field.py --field-structure symmetric</code>), 300 to 309 eV,   urad's single-electron field matches with either end model as closely as   with hard ends: ring radius to 1 um, rms deviation 0.8-2.8 % of the peak,   and the absolute flux density 2-3.5 % high, as it was before -- the   tracking step of section 12, since corrected. The ring   moves with the ends as it should -- at 307 eV the exact integral puts it at   361.4, 355.1 and 348.7 um for hard, 1/4-3/4 and soft ends, urad at 362.3,   356.2 and 349.7 um.</li> <li>A trace on a 151 x 151 pinhole grid takes 1.72-1.83 s instead of 0.97 s   without the energy spread, and 3.50 s instead of 2.18 s with it folded in   over nine energies.</li> <li>In the source plane, with the energy spread folded in and the same field on   both sides (<code>qmap_source_three_way.py --ends quarter|soft --field-structure   symmetric</code>, 290 to 310 eV, +-40 um window), urad and SRW agree with either   end model as closely as with hard ends: urad/SRW 0.98-1.04 up to 301 eV and   0.99-1.11 above, energy by energy the values of the hard-ended comparison.   The ends themselves move urad's window by at most 2.4 % up to 301 eV, and   above it by 0.94-1.09 with 1/4-3/4 poles and 0.89-1.22 with soft ends.</li> </ul>"},{"location":"FieldPropagationInUrad.html#12-the-tracking-step","title":"12. The tracking step","text":"<p>urad tracks one period in steps of <code>step</code> mm (<code>urad_e_b_field</code> with <code>uradstep_omp</code>: an exact helix in the field at the straight-line midpoint) and integrates the radiation over each step with the exact phase, advanced by <code>(1 - n.beta)*dt</code>, but with the Li\u00e9nard-Wiechert amplitude held at the end of the step. That amplitude is not slowly varying. It goes with <code>1/(1 - n.beta)^2</code>, and <code>1 - n.beta</code> swings between <code>1/(2 gamma^2)</code> where the orbit runs parallel to the axis -- at every pole -- and <code>(1+K^2)/(2 gamma^2)</code> in between. The peaks are about <code>lambda_u/(2 pi K)</code> wide, 5 mm on the UE65.</p> <p>RAY-UI passed a fixed step of 1 mm, and the on-axis flux of the UE65 came out 2.36 % above the textbook, at 1000 m as at 10 m. A replica of urad's quadrature (QMAP project, <code>srw/scripts/qmap_urad_quadrature.py</code>) reproduces the trace to 3.5e-5 at 1 mm and converges on the textbook, to 4e-6 at 0.02 mm, as the step shrinks. The error falls with the square of the step and grows steeply with K (first harmonic, 65.6 mm period, at the resonance):</p> K 1 mm 0.5 mm 0.2 mm 0.1 mm 1 +0.28 % +0.035 % +0.005 % +0.001 % 2 +2.35 % +0.30 % +0.042 % +0.010 % 3 +7.66 % +0.69 % +0.088 % +0.021 % 4 +19.1 % +1.25 % +0.14 % +0.033 % 5 +42.4 % +2.13 % +0.19 % +0.045 % <p>The third harmonic is less sensitive (K = 2: +1.4 % at 1 mm). urad's own physical constants, the CODATA values of 1998, play no part in the normalisation: they move it by 1e-7. One of them does matter at high harmonics; see the end of this section.</p> <p>At high harmonics a second limit takes over. The radiated phase turns <code>E/E1</code> times per period, and the field of a harmonic is what is left after the contributions from across the period cancel: the weaker the harmonic, the more of the step error survives. At a fixed number of steps per period the error therefore grows with the harmonic, the faster the smaller K. Error against the textbook <code>F_h(K)</code>, with twenty-five steps across a peak alone (<code>157 max(K,1)</code> per period) and with thirty steps per turn of the phase added:</p> K h = 5 h = 9 h = 13 h = 17 h = 21 1 <code>F_h</code> 5.5e-2 3.6e-3 1.9e-4 9.3e-6 4.3e-7 peaks -0.09 % +0.13 % +1.6 % +8.9 % +46 % peaks and phase -0.09 % +0.04 % +0.26 % +0.84 % +2.7 % 1.5 <code>F_h</code> 0.26 0.089 0.025 6.5e-3 1.6e-3 peaks -0.01 % -0.00 % +0.09 % +0.35 % +0.95 % peaks and phase -0.01 % -0.02 % +0.02 % +0.06 % +0.11 % 2 <code>F_h</code> 0.41 0.29 0.17 0.089 0.045 peaks +0.01 % -0.00 % +0.00 % +0.03 % +0.09 % peaks and phase +0.01 % -0.00 % +0.00 % +0.01 % +0.02 % 2.5 <code>F_h</code> 0.43 0.41 0.34 0.26 0.19 peaks +0.03 % +0.02 % +0.02 % +0.03 % +0.04 % peaks and phase +0.03 % +0.02 % +0.02 % +0.01 % +0.01 % <p>The step error depends only on K, the harmonic and the steps per period, not on the electron energy or the period length. With a 17 mm period at 2.5 GeV these harmonics lie between 6 and 49 keV; with the old 1 mm step they were off by up to four orders of magnitude.</p> <p><code>initUradPhase</code> now sets the number of steps per period to <code>max(2 pi max(K,1) 25, 30 E/E1)</code>: twenty-five steps across a peak and thirty per turn of the radiated phase. K and E1 are taken as urad takes them, from the fundamental <code>harm/nharm</code> or else from the field, and E is the highest photon energy traced. For K from 0.5 to 3 and harmonics up to the 25th the error stays within 0.12 % wherever <code>F_h(K) &gt;= 1e-3</code>, down to harmonics some four hundred times weaker than the strongest; below that it grows (K = 1, h = 21, <code>F_h</code> = 4e-7: 2.7 %). On the UE65 at its first harmonic that is 0.208 mm, 315 steps per period instead of 67, and 630 at its 21st. Every observation point tracks them: a trace of the QMAP beamline (99 x 71 pinhole points, 1/4-3/4 ends, 200 000 rays) takes 12.2 s with the old step, 13.5 s with 315 steps per period and 14.7 s with 630, 4.5-5 ms per step per period. <code>RAYUI_URAD_STEP</code> (mm) overrides the rule. The selftest suite checks the on-axis flux against the textbook at K = 2 and K = 3 on the first harmonic and at K = 1.5 on the 21st (<code>test/regression/tests/selftest/undulatorsource</code>).</p> <p>That last case exposed the one constant that matters. urad derived K from the K-value energy with <code>WTOE1</code>, which was built on <code>HPLANCK1 = 6.626176e-34</code>, the CODATA value of 1973, while it radiates with <code>HBAREV1</code> of 1998. The harmonic therefore sat 1.6e-5 below the energy it was set for -- 8939.855 eV for 8940 -- which is nothing at the first harmonic but 2.4 % of the line width at the 21st of 71 periods, and put the on-axis flux there 0.42 % high at any step (1.00422 converged, against 1.00113 at the resonance with the rule's 630 steps). <code>HPLANCK1</code> in <code>phyconparam.cmn</code> is now <code>2 pi HBAREV1 ECHARGE1</code>, the CODATA 1998 value. RAY-UI's C++ uses the exact SI values of 2019, which differ from urad's by 2e-7.</p>"},{"location":"FieldPropagationInUrad.html#13-open-ray-directions-from-the-angular-spectrum","title":"13. Open: ray directions from the angular spectrum","text":"<p>Not implemented -- a note of what the measurements point to.</p> <p>A ray's direction is taken from where it crosses the pinhole, <code>x/L</code>. That position also carries where the ray started, so the directions come out with the source size folded in once more, in quadrature: <code>sqrt(sigma'^2 + (sigma/L)^2)</code>. The ray origins carry the source size already, so it is counted twice. On the QMAP source at 800 eV the horizontal direction core is 12.00 urad with the pinhole at 10 m and 11.73 urad at 25 m; <code>sqrt(11.73^2 + (25.3 um / 10 m)^2) = 12.00</code>, and the far-field value is about 11.69 urad. At 10 m the angular distribution is some 2 % too wide horizontally, less vertically, where the source is 19 um.</p> <p>Part of that is urad's own fold. The far-field Stokes parameters are folded with <code>sqrt(beta*eps + L^2*eps/beta)</code>, the electron beam's size at the pinhole, which is right for the intensity there but carries the electron size into the angles; for directions only the divergence term belongs. The other part is the single electron's own source size.</p> <p>Moving the pinhole out is the expensive way round it: at 25 m the derivation asks for six times the points, and urad's field computation, about 0.3 ms a point, goes from 5 to 32 s.</p> <p>The cheap way is the angular spectrum. The single-electron field on the pinhole fixes the field everywhere downstream, and its plane-wave decomposition -- the Fourier transform of the field with the reference sphere taken out -- is the far field at infinity, whatever the pinhole distance. The transform of section 3 forms that product on its way to the source plane. Drawing directions from its square, with only the electron divergence folded in, would remove the double counting without moving the pinhole. The same step run forward would give the field at any plane downstream, the first mirror at 25 m for instance, from the 10 m field in milliseconds.</p> <p>To be settled before building it: how the fold of the electron divergence is split from the size fold in urad, and whether origins and directions stay sampled independently, as they are now, or are drawn together.</p>"},{"location":"FieldPropagationInUrad.html#14-threads","title":"14. Threads","text":"<p>urad's field computation and the direct sum are OpenMP loops, but their <code>!$OMP</code> directives stay comments unless the files are compiled with <code>-fopenmp</code>. <code>Ray-UI.pro</code> compiles the four urad files, and only those, with it through rules of their own (<code>urad_omp_module</code>, <code>urad_omp</code>): <code>-fopenmp</code> implies <code>-frecursive</code>, which puts every local variable on the stack, and the legacy raycore sources are not written for that.</p> <p>The loop over electrons and pinhole points is collapsed (<code>COLLAPSE(2)</code>, static chunks), so with the single electron UndulatorSource uses, the points are shared out. Each point is written by one thread only, and the grids are bit-identical for any number of threads. The per-thread copy of the field (<code>fpriv</code>) is allocatable, so it lives on the heap rather than on the 512 KB stack macOS gives a secondary thread.</p> <p>The number of threads comes from <code>uradThreadCount</code> in <code>undulatorsource.cpp</code>: - one when the trace's multithreading preference is off, or when a Looper job   runs beside others; - otherwise <code>OMP_NUM_THREADS</code> where it is set; - otherwise the performance cores on macOS (<code>sysctl</code> <code>hw.perflevel0.physicalcpu</code>) and <code>QThread::idealThreadCount()</code> elsewhere.</p> <p>The first trace of a batch session reads the Background preference. On an Apple M-series Mac with eight performance cores, urad's share of a QMAP trace at 800 eV with nine energy-spread nodes drops from 6.3 s on one thread to 1.25 s on eight.</p>"},{"location":"FieldPropagationInUrad.html#on-linux-and-windows","title":"On Linux and Windows","text":"<p>State of 2026-09-13; built and run on macOS only so far.</p> <ul> <li>Build. The rules call <code>$(CC) -c $(CFLAGS) -fopenmp $(INCPATH)</code>, the same   compiler and flags qmake uses for every other Fortran file. That is gfortran   on macOS (<code>QMAKE_CC</code>) and <code>gcc</code> on Linux and MinGW, which hands <code>.f</code> files   to gfortran. qmake's Unix and MinGW generators both define <code>CC</code>, <code>CFLAGS</code>   and <code>INCPATH</code>. The OpenMP runtime was linked on all three platforms before:   <code>LIBS += -fopenmp</code> for Linux and Windows, <code>QMAKE_LFLAGS += -fopenmp</code> on   macOS.</li> <li>Code. The only platform-specific part is the macOS core count, behind   <code>#ifdef __APPLE__</code>. The preference switch is Qt, and the Fortran side is   standard OpenMP 3.0 (<code>COLLAPSE</code>, private allocatables), which every   gfortran from 4.4 on supports.</li> <li>Stack. <code>-frecursive</code> puts urad's locals on the stack. macOS gives   secondary threads the least, 512 KB, and runs; Linux (typically 8 MB) and   Windows (1 to 2 MB) give more.</li> <li>Verification. The Linux CI builds with Ubuntu's gfortran and runs every   selftest suite on each pull request, so Linux is checked there. Windows has   no CI and needs one MinGW build and a trace by hand.</li> </ul> <p>Open: on Linux and Windows the thread count includes the efficiency cores of CPUs that have them, Intel from the 12th generation on. urad gives every thread an equal, fixed share of the points, so an efficiency core holds the others up. Until the performance cores are detected there too (<code>/sys/devices/cpu_core/cpus</code> on Linux, <code>GetLogicalProcessorInformationEx</code> with <code>EfficiencyClass</code> on Windows), <code>OMP_NUM_THREADS</code> sets the count by hand.</p>"},{"location":"FieldPropagationInUrad.html#15-the-flux-optimised-photon-energy","title":"15. The flux-optimised photon energy","text":"<p>On \"auto\", Photon Energy is the harmonic energy, where the flux on the axis peaks, and with it the brilliance. The flux through an aperture peaks lower. Below the harmonic energy the resonance leaves the axis for a ring, <code>E_n(theta) = E_n/(1 + gamma^2 theta^2/(1 + K^2/2))</code>. An aperture wider than the central cone collects more of it the lower the energy, until the line has moved down by about one line width <code>E_n/(nN)</code>; further down, the amplitude off the axis falls. Auto Photon Energy chooses between the two: \"harmonic energy\" or \"flux maximum through pinhole\".</p> <p>The flux maximum is found for the source's own pinhole, derived at the harmonic energy or entered by hand (<code>fluxMaximumEnergy</code>): - One urad run. It covers 25 evenly spaced energies, from 2.5 line   widths below the harmonic energy to 0.5 above, widened by three sigma of the   energy spread. It uses 21 x 21 points over the pinhole and skips the   back-propagation. Every further energy costs urad a few percent of the   first. - Flux per energy. The flux of every energy is summed by the trapezoid   rule. When the trace folds the energy spread in, the spread is applied as a   Gaussian convolution of that curve, so the energy found is the maximum of the   flux RAY-UI reports. - Maximum. A parabola through the largest value and its two neighbours   places the maximum. A maximum at an end of the scan moves the scan.</p> <p>The result is kept until something urad is given changes, so repeated auto-calculations do not search again. The search runs before the trace sets up urad, which is why <code>initUradPhase</code> calls <code>calcPhotonEnergy</code> first.</p> <p>urad keeps its settings and its results in globals, so a trace and the search take turns on one lock (<code>uradMutex</code>). This matters in the window. While a trace runs on its own thread, the window re-runs the auto-calculation of the beamline on the main thread (<code>propagateAutoCalculatedParameters</code>). The first version of the search then rewrote urad's grid sizes under the running trace, whose electron-beam fold (<code>urad_fold_gauss_fft</code>) sized its arrays from them and asked for gigabytes. Now the trace holds the lock from <code>initUradPhase</code> to the end of its urad run. The search only tries for the lock, and while a trace holds it the last result stands. The search also writes nothing of the source: it takes the harmonic energy's pinhole from <code>derivePinHole</code> and sets urad up through <code>setUpUrad</code>, the part of <code>initUradPhase</code> that only fills urad's globals.</p> <p>Against a reference on the full grid (141 x 121 and 161 x 121 points) at 89 energies:</p> <pre><code>                                 harmonic   flux maximum   reference   flux gained\nUE65 QMAP, 3rd harmonic, 10 m    800 eV\n    energy spread folded in                  795.96 eV     796.02 eV     +76 %\n    without                                  797.24 eV     797.27 eV     +84 %\nU49 BESSY II, 3rd harmonic, 4 m  300 eV\n    energy spread folded in                  298.83 eV     298.84 eV     +74 %\n    without                                  299.15 eV     299.13 eV     +79 %\n</code></pre> <p>The flux gained is that through the pinhole, against the harmonic energy. Coarser grids fail first on the flux: 31 and 41 points give the same maximum as 21, while 15 and fewer miss the flux by several percent. On one thread, one search takes about 0.26 s for the UE65 at 800 eV and about 0.09 s for the U49, and loading a file takes that much longer than with the harmonic energy. The search runs once per load. While a file loads, the source takes over the accelerator, the segment and the device in turn, each followed by an auto-calculation, and a search at each step would run on inputs that are not yet complete; two ran, and loading took twice as long. So the energy the file was saved with stands until the load is done, and the first auto-calculation after it searches.</p> <p>The selftest checks the property rather than a number. On the UE65 at its first harmonic (300 eV, 2.4 mm pinhole at 10 m) the search finds 296.45 eV. Traced by hand 1 eV below and above, the flux is 1.1 % and 2.0 % lower. With the energy spread folded in, the search finds 296.24 eV.</p>"},{"location":"FieldPropagationInUrad.html#references","title":"References","text":"<ul> <li>J. W. Goodman, Introduction to Fourier Optics, 3rd ed., Roberts &amp; Company,   2005 \u2014 Fresnel diffraction and the transform form of the diffraction integral.</li> <li>D. G. Voelz, Computational Fourier Optics: A MATLAB Tutorial, SPIE Press,   2011 \u2014 the one-step and two-step Fresnel propagators, the angular-spectrum   method, and the sampling conditions that decide which is usable.</li> <li>M. Born and E. Wolf, Principles of Optics, 7th ed., Cambridge University   Press, 1999 \u2014 the Kirchhoff integral the discretised sum comes from.</li> <li>J. W. Cooley and J. W. Tukey, An algorithm for the machine calculation of   complex Fourier series, Math. Comput. 19, 297 (1965).</li> <li>O. Chubar and P. Elleaume, Accurate and efficient computation of synchrotron   radiation in the near field region, Proc. EPAC'98, 1177 (1998) \u2014 SRW.</li> <li>T. Tanaka and H. Kitamura, SPECTRA: a synchrotron radiation calculation   code, J. Synchrotron Rad. 8, 1221 (2001).</li> <li>M. Scheer, urad / WAVE \u2014 the direct sum implemented in   <code>src/uradphase/urad_phase_main.f</code>.</li> <li>J. A. Clarke, The Science and Technology of Undulators and Wigglers,   Oxford University Press, 2004, section 7.5.1 (pp. 121-124) \u2014 end   terminations, among them the 1/4, -3/4, 1, -1 structure.</li> <li>R. P. Walker, Advanced insertion devices, Proc. EPAC'94, 310 (1994), and   B. Diviacco and R. P. Walker, Pure permanent magnet end-configurations,   Sincrotrone Trieste ST/M-TN-92/5 (1992) \u2014 Clarke's sources for it.</li> <li>E. Gluskin et al., Optimization of the design for the LCLS undulator   line, ANL/XFD/CP-102080 \u2014 pole strengths 0.25, 0.75, 1 at the ends, for   no angle kick and no offset.</li> <li>O. Chubar, SRW, <code>cpp/src/core/srmagfld.h</code>, <code>srTMagFieldPeriodic::compB</code> \u2014   the soft termination.</li> </ul>"},{"location":"FieldPropagationInUrad.html#where-the-code-is","title":"Where the code is","text":"direct sum <code>src/uradphase/urad_phase_main.f</code>, <code>urad_phase_prop_classic</code> transform <code>src/uradphase/urad_phase_prop_fft.f</code>, <code>urad_phase_prop_fft</code> comparison same file, <code>urad_phase_prop_check</code> grid read-back <code>src/Ray-UI/LightSources/undulatorsource.cpp</code> undulator ends <code>src/uradphase/urad_phase_main.f</code>, <code>urad_end_profile</code>, <code>urad_end_setup</code>, the end sections in <code>urad_amprep</code> tracking step <code>src/Ray-UI/LightSources/undulatorsource.cpp</code>, <code>initUradPhase</code>; used in <code>urad_e_b_field</code> threads <code>src/Ray-UI/Ray-UI.pro</code>, <code>urad_omp_module</code> and <code>urad_omp</code>; <code>src/Ray-UI/LightSources/undulatorsource.cpp</code>, <code>uradThreadCount</code>; the parallel loop in <code>urad_amprep</code> flux maximum <code>src/Ray-UI/LightSources/undulatorsource.cpp</code>, <code>fluxMaximumEnergy</code> and <code>calcPhotonEnergy</code>"},{"location":"LooperPresetFormat.html","title":"Looper Preset JSON Format (<code>*.rayloop.json</code>)","text":"<p>This document describes the JSON format used by the Looper preset import/export in RAY-UI.</p>"},{"location":"LooperPresetFormat.html#purpose","title":"Purpose","text":"<p>A Looper preset stores: - loop table columns (mapped to beamline parameters), - loop table rows (values + enabled state), - execution settings, - output/export settings.</p> <p>The file is used by: - GUI: Load/Save Looper Preset, - CLI background mode: <code>looper validate \"&lt;preset-file&gt;\"</code>, <code>looper load \"&lt;preset-file&gt;\"</code>, <code>looper run \"&lt;preset-file&gt;\"</code>.</p>"},{"location":"LooperPresetFormat.html#required-schema-and-version","title":"Required Schema and Version","text":"<ul> <li><code>schema</code> must be exactly <code>\"rayui.looper.preset\"</code></li> <li><code>version</code> must be exactly <code>1</code></li> </ul> <p>Any other schema/version is rejected.</p>"},{"location":"LooperPresetFormat.html#minimal-valid-example","title":"Minimal Valid Example","text":"<pre><code>{\n  \"schema\": \"rayui.looper.preset\",\n  \"version\": 1,\n  \"columns\": [\n    {\n      \"elementName\": \"PG\",\n      \"parameterId\": \"orderDiffraction\",\n      \"unit\": \"\"\n    }\n  ],\n  \"rows\": [\n    {\n      \"enabled\": true,\n      \"values\": [\"1\"]\n    }\n  ]\n}\n</code></pre>"},{"location":"LooperPresetFormat.html#full-example","title":"Full Example","text":"<pre><code>{\n  \"schema\": \"rayui.looper.preset\",\n  \"version\": 1,\n  \"beamline\": {\n    \"filePath\": \"/path/to/beamline.rml\",\n    \"fileName\": \"beamline.rml\"\n  },\n  \"execution\": {\n    \"inputMode\": \"table\",\n    \"loopMode\": \"cartesian\",\n    \"maxParallelJobs\": 0\n  },\n  \"columns\": [\n    {\n      \"elementName\": \"Monochromator\",\n      \"parameterId\": \"photonEnergy\",\n      \"unit\": \"eV\"\n    },\n    {\n      \"elementName\": \"Undulator\",\n      \"parameterId\": \"harmonic\",\n      \"unit\": \"\"\n    }\n  ],\n  \"rows\": [\n    {\n      \"enabled\": true,\n      \"values\": [\"500\", \"1\"]\n    },\n    {\n      \"enabled\": false,\n      \"values\": [\"600\", \"3\"]\n    }\n  ],\n  \"output\": {\n    \"outputDir\": \"/tmp/ray-looper-out\",\n    \"includeLooperColumns\": true,\n    \"writeLoopDataCsv\": true,\n    \"useParamValuesInFileNames\": false,\n    \"saveScanRmlFiles\": false,\n    \"exportEnabledKeys\": [\"ImagePlane:RawRaysBeam\", \"ImagePlane:FootprintAllRays\"],\n    \"exportCsvSeparator\": \"\\t\",\n    \"exportCsvDecimalSeparator\": \";\"\n  },\n  \"exporter\": {\n    \"csvSeparator\": \"\\t\",\n    \"csvDecimalSeparator\": \";\",\n    \"enabledKeys\": [\"ImagePlane:RawRaysBeam\", \"ImagePlane:FootprintAllRays\"]\n  }\n}\n</code></pre>"},{"location":"LooperPresetFormat.html#field-reference","title":"Field Reference","text":"<p>Top-level fields: - <code>schema</code> (string, required): fixed value <code>\"rayui.looper.preset\"</code>. - <code>version</code> (integer, required): fixed value <code>1</code>. - <code>beamline</code> (object, optional): metadata. - <code>execution</code> (object, optional): looper execution settings. - <code>columns</code> (array, required, non-empty): parameter columns. - <code>rows</code> (array, required, non-empty): run rows. - <code>output</code> (object, optional): output settings. - <code>exporter</code> (object, optional): exporter settings.</p> <p><code>beamline</code> object: - <code>filePath</code> (string, optional) - <code>fileName</code> (string, optional)</p> <p><code>execution</code> object: - <code>inputMode</code> (string, optional): <code>\"table\"</code> or <code>\"parameter\"</code>, defaults to <code>\"table\"</code> if invalid/missing. - <code>loopMode</code> (string, optional): <code>\"cartesian\"</code> or <code>\"parallel_zip_shortest\"</code>, defaults to <code>\"cartesian\"</code> if invalid/missing. - <code>maxParallelJobs</code> (integer, optional): values <code>&lt; 0</code> are clamped to <code>0</code> (<code>0</code> means auto/default behavior).</p> <p><code>columns[]</code> item: - <code>elementName</code> (string, required, non-empty) - <code>parameterId</code> (string, required, non-empty) - <code>unit</code> (string, optional)</p> <p><code>rows[]</code> item: - <code>enabled</code> (boolean, optional, default <code>true</code>) - <code>values</code> (array, required)</p> <p>Validation rule: - each <code>rows[i].values</code> length must be exactly equal to <code>columns.length</code>.</p> <p>Value conversion rule: - string/number/bool JSON values in <code>rows[i].values</code> are accepted and converted to strings internally.</p> <p><code>output</code> object: - <code>outputDir</code> (string, optional) - <code>includeLooperColumns</code> (boolean, optional, default <code>true</code>) - <code>writeLoopDataCsv</code> (boolean, optional, default <code>true</code>) - <code>useParamValuesInFileNames</code> (boolean, optional, default <code>false</code>) - <code>saveScanRmlFiles</code> (boolean, optional, default <code>false</code>): writes one <code>scan_####.rml</code> per scan into each scan folder. - <code>exportEnabledKeys</code> (array of strings, optional): each entry is   <code>\"&lt;objectName&gt;:&lt;itemId&gt;\"</code>. The object prefix is required for the   per-object exports -- footprints, raw rays, flux densities, beam properties --   because the exporter keys them by object and item together. A bare <code>\"itemId\"</code>   is accepted by the parser but selects nothing: the run then reports   \"Warning: no per-object output items selected\" and produces scan folders   with no exports in them. Valid item ids are the ones <code>export items</code> lists in   background mode, for example:</p> <p><pre><code>\"exportEnabledKeys\": [\"Focus:FootprintOutgoingRays\",\n                      \"ImagePlane:ScalarBeamProperties\"]\n</code></pre> - <code>exportCsvSeparator</code> (string, optional) - <code>exportCsvDecimalSeparator</code> (string, optional)</p> <p><code>exporter</code> object: - <code>csvSeparator</code> (string, optional) - <code>csvDecimalSeparator</code> (string, optional) - <code>enabledKeys</code> (array of strings, optional): same <code>\"&lt;objectName&gt;:&lt;itemId&gt;\"</code>   form as <code>output.exportEnabledKeys</code>.</p>"},{"location":"LooperPresetFormat.html#notes-on-output-vs-exporter","title":"Notes on <code>output</code> vs <code>exporter</code>","text":"<p>Both sections can contain exporter-related settings. - Newer preset writing includes exporter settings in <code>output</code>. - <code>exporter</code> is also supported for compatibility.</p> <p>When both are present during loading: - values from <code>exporter</code> are applied after <code>output</code> and therefore take precedence.</p>"},{"location":"LooperPresetFormat.html#validation-in-cli","title":"Validation in CLI","text":"<p>Use background mode:</p> <pre><code>Ray-UI -b\nlooper validate \"&lt;preset-file&gt;\"\n</code></pre> <p><code>looper validate</code> checks JSON/schema/version and column/row consistency. If a beamline is loaded, it also checks whether referenced parameters exist.</p> <p>To execute a preset in background mode:</p> <pre><code>Ray-UI -b\nlooper run \"&lt;preset-file&gt;\"\n</code></pre>"},{"location":"SurfaceScattering.html","title":"Surface errors and aperture diffraction of mirrors","text":"<p>A ray tracer sees a mirror's height errors only through the tilt they give the local normal, and nothing diffracts at a mirror's edges. For X-ray mirrors of good quality both simplifications can be wrong by large factors, in opposite directions and at opposite ends of the photon-energy range. This note describes two per-mirror settings that replace them with a wave-optically correct treatment: the physics, the implementation, how to use them, and what they do not cover.</p> setting (RML id) values what it does Profile Treatment (<code>surfaceErrorTreatment</code>) <code>geometric (local slope)</code> (0, default) \u00b7 <code>wave-optical (PSD scattering)</code> (1) how a surface profile file acts on the rays Edge Diffraction (Fraunhofer) (<code>edgeDiffraction</code>) <code>off</code> (0, default) \u00b7 <code>on</code> (1) Fraunhofer diffraction at the mirror aperture <p>Both settings exist for the plane mirror, cylinder, cone, sphere, toroid, ellipsoid, hyperboloid, paraboloid, elliptical toroid and the experts optics, and for the plane, spherical and toroidal gratings (section 2.4). Crystals, capillaries and the reflection zone plate keep the geometric treatment. A file without the settings traces exactly as before.</p>"},{"location":"SurfaceScattering.html#1-where-the-geometric-model-goes-wrong","title":"1. Where the geometric model goes wrong","text":"<p>The QMAP beamline at BESSY III was simulated in RAY-UI and, independently, with the wave-optics code SRW, with ideal mirrors up to the exit slit and the same measured-like surface profiles on the two focusing mirrors M5 and M6 (horizontal and vertical FWHM, 0.07 % photon band in both codes):</p> 400 eV RAY-UI 400 eV SRW 1000 eV RAY-UI 1000 eV SRW exit slit, vertical 15.00 \u00b5m 15.10 \u00b5m 15.00 \u00b5m 15.09 \u00b5m focus, horizontal 142.3 nm 59.7 nm 161.8 nm 42.0 nm focus, vertical 94.3 nm 86.7 nm 104.9 nm 94.8 nm <p>Up to the exit slit, where no mirror carries a profile, the codes agree. The horizontal focus differs by a factor of 2.4 to 3.9, and the difference is generated at M5 and M6. The geometric width hardly depends on the photon energy; the wave-optical one falls with the wavelength.</p> <p>The reason is the size of the phase error. A height error <code>h</code> imprints on a beam reflected at grazing angle <code>theta</code> the phase</p> <pre><code>phi = 2 k h sin(theta) = 4 pi h sin(theta) / lambda\nsigma_phi = 4 pi sigma_h sin(theta) / lambda          (rms)\n</code></pre> <p>The M6 profile has <code>sigma_h</code> = 0.25 nm at <code>theta</code> = 4\u00b0, which gives <code>sigma_phi</code> = 0.07 rad at 400 eV and 0.18 rad at 1000 eV. A wavefront with 0.1 rad of phase error is almost perfect: its Strehl ratio <code>exp(-sigma_phi^2)</code> is 0.99 to 0.97, the focus keeps its diffraction-limited core, and a few percent of the power go into a faint, wide halo. Geometric optics instead deflects every ray by twice the local slope, however small the height behind that slope, and turns the same surface into a blur of the full slope width. That is the limit <code>sigma_phi &gt;&gt; 1</code>, and it does not apply here.</p> <p>At low energy the opposite happens. M6 is overfilled up to about 200 eV, its 200 mm length limits the numerical aperture, and the focus is the Fraunhofer pattern of the mirror aperture. A ray tracer has no diffraction at a mirror edge and returns a focus below the diffraction limit (at 50 eV RAY-UI gives 160 nm where the diffraction limit of M6 alone is about 270 nm).</p>"},{"location":"SurfaceScattering.html#2-scattering-from-a-surface-with-height-errors","title":"2. Scattering from a surface with height errors","text":""},{"location":"SurfaceScattering.html#21-the-surface-as-a-phase-screen","title":"2.1 The surface as a phase screen","text":"<p>In the Kirchhoff (tangent-plane) approximation the reflected field is the incident one times <code>exp(i phi(x, z))</code>. For a surface whose heights are a stationary Gaussian random process with autocovariance <code>C(tau) = &lt;h(r) h(r + tau)&gt;</code> and variance <code>sigma_h^2 = C(0)</code>, the angular distribution of the reflected intensity is the Fourier transform, over the separation <code>tau</code>, of the surface transfer function</p> <pre><code>H(tau) = &lt; exp(i [phi(r + tau) - phi(r)]) &gt; = exp( -sigma_phi^2 [1 - C(tau) / sigma_h^2] )\n</code></pre> <p>This is the Beckmann\u2013Kirchhoff result [1], in the form Harvey and co-workers use for optical surfaces [2, 3] and Sinha et al. derive for X-rays at grazing incidence [4]. Spatial frequency <code>f</code> on the surface corresponds to a change of direction by the grating equation (section 2.3).</p>"},{"location":"SurfaceScattering.html#22-the-expansion-that-becomes-a-monte-carlo","title":"2.2 The expansion that becomes a Monte Carlo","text":"<p>Expanding the exponential,</p> <pre><code>H(tau) = exp(-sigma_phi^2) * sum_{n &gt;= 0} (sigma_phi^2)^n / n! * [C(tau) / sigma_h^2]^n\n</code></pre> <p>The Fourier transform of <code>C(tau) / sigma_h^2</code> is the power spectral density normalised to unit integral, <code>p(f) = PSD(f) / sigma_h^2</code>, which is a probability density; the transform of its n-th power is the n-fold convolution of <code>p</code> with itself. The reflected intensity is therefore a Poisson mixture: with probability</p> <pre><code>P(n) = exp(-sigma_phi^2) (sigma_phi^2)^n / n!\n</code></pre> <p>the ray carries the sum of <code>n</code> spatial frequencies drawn independently from <code>p(f)</code>. This is the algorithm RAY-UI uses, ray by ray. It contains the familiar limits:</p> <ul> <li><code>n = 0</code>, weight <code>S = exp(-sigma_phi^2)</code>: the specular core, reduced by the   Debye\u2013Waller or Strehl factor. <code>1 - S</code> is the total integrated scatter of   Bennett and Porteus [5].</li> <li><code>sigma_phi &lt;&lt; 1</code>: after the core, <code>n = 1</code> dominates, and the halo has the shape   of the PSD. This is first-order (Rayleigh\u2013Rice) scattering [6, 7].</li> <li><code>sigma_phi &gt;&gt; 1</code>: the sum of many frequencies is Gaussian, with covariance   <code>sigma_phi^2 &lt;f f&gt;</code>. Section 2.3 shows that this is exactly the geometric   deflection by twice the slope.</li> </ul> <p>The variance of the direction change is <code>sigma_phi^2 &lt;f^2&gt; lambda^2</code> for every <code>sigma_phi</code>: the rms deflection is always twice the rms slope. What changes with <code>sigma_phi</code> is the shape. A small phase error keeps most rays undeflected and sends a few far out; a large one deflects all rays a little. The rms width of a focus therefore hardly changes between the two treatments, while its FWHM and its encircled energy can change by a large factor.</p>"},{"location":"SurfaceScattering.html#23-direction-change-at-grazing-incidence","title":"2.3 Direction change at grazing incidence","text":"<p>A spatial frequency <code>(f_x, f_z)</code> of the surface, <code>z</code> along the mirror and <code>x</code> across it, adds <code>lambda (f_x, f_z)</code> to the tangential components of the ray's unit direction vector. That is the grating equation of that Fourier component:</p> <pre><code>cos(beta') = cos(beta) + lambda f_z      -&gt; delta_beta = -lambda f_z / sin(theta)   (in the plane of incidence)\nsin(alpha') = lambda f_x                 -&gt; delta_alpha = lambda f_x                (across it)\n</code></pre> <p>In the plane of incidence the change is amplified by <code>1 / sin(theta)</code>; across it, it is not. In the limit <code>sigma_phi &gt;&gt; 1</code> the rms tangential change is</p> <pre><code>lambda sigma_phi sqrt(&lt;f_z^2&gt;) = 4 pi sigma_h sin(theta) sqrt(&lt;f_z^2&gt;) = 2 sin(theta) m_z\n</code></pre> <p>with <code>m_z = 2 pi sigma_h sqrt(&lt;f_z^2&gt;)</code> the rms meridional slope. In the plane of incidence that is a deflection of <code>2 m_z</code>, and across it <code>2 sin(theta) m_x</code>: exactly the geometric rules, including the well-known suppression of sagittal slope errors at grazing incidence.</p>"},{"location":"SurfaceScattering.html#24-gratings","title":"2.4 Gratings","text":"<p>A height error <code>h</code> of a grating moves its grooves along the normal. The incoming wave and the diffracted order then travel <code>h sin(alpha)</code> and <code>h sin(beta)</code> further, with <code>alpha</code> and <code>beta</code> their grazing angles, so</p> <pre><code>phi = k h (sin(alpha) + sin(beta)),   sigma_phi = 2 pi sigma_h (sin(alpha) + sin(beta)) / lambda\n</code></pre> <p>For a mirror, <code>alpha = beta</code>, and this is the formula of section 1. The rest is unchanged. A spatial frequency <code>f</code> of the surface adds <code>lambda f</code> to the tangential direction of the diffracted ray: the grating equation of that Fourier component, on top of that of the grooves. The table, the Poisson expansion and its limits are those of sections 2.1 to 2.3; only <code>mu</code> takes both angles. A ray sent into the zeroth order has <code>alpha = beta</code> and gets the mirror formula.</p> <p>Edge diffraction on a grating is diffraction at the aperture the diffracted order sees, <code>L sin(beta)</code> in the dispersion plane. Through the dispersion it is the resolving power of the grating. At fixed incidence the grating equation <code>cos(alpha) - cos(beta) = m N lambda</code> gives <code>sin(beta) d(beta) = m N d(lambda)</code>, with <code>m</code> the order and <code>N</code> the line density. The first zero of the Fraunhofer pattern, <code>d(beta) = lambda / (L sin(beta))</code>, is therefore <code>d(lambda) = lambda / (m N L)</code>. That is Rayleigh's <code>lambda / d(lambda) = m N L</code>, the order times the number of illuminated grooves [12]. A ray tracer without it resolves as finely as the source size and the slope errors allow. As on a mirror, the setting belongs on a grating that the beam overfills; an underfilled grating's resolution is already in the divergence of the rays.</p>"},{"location":"SurfaceScattering.html#3-implementation","title":"3. Implementation","text":"<p>The code is <code>src/raycore/surface_scattering.f90</code>. RAY.FOR calls it at four places.</p>"},{"location":"SurfaceScattering.html#31-the-table-of-a-mirror","title":"3.1 The table of a mirror","text":"<p>When <code>SURF</code> has read a profile file for a mirror whose slope errors are on and whose Profile Treatment is wave-optical, <code>SURFSCAT_SETUP</code> builds a table:</p> <ol> <li>It removes piston and the best-fit plane; they belong to the alignment, not    to the surface error.</li> <li><code>sigma_h</code> is the rms of the residual.</li> <li>It applies a Welch window along both axes and zero-pads to the next power of    two at least twice the profile size. Without the window, the edge    discontinuity of a non-periodic measured map would leak into the high    frequencies and appear as a spurious halo.</li> <li>The periodogram <code>|FFT|^2</code> without its DC term, normalised to one, becomes a    cumulative table over the frequency grid, together with the second moments    <code>&lt;f_z^2&gt;</code> and <code>&lt;f_x^2&gt;</code>.</li> </ol>"},{"location":"SurfaceScattering.html#32-each-reflected-ray","title":"3.2 Each reflected ray","text":"<p>After <code>REFLEX</code>, <code>SURFSCAT_APPLY</code> computes, from the photon energy and the local grazing angle, <code>mu = sigma_phi^2</code>.</p> <ul> <li>For <code>mu &lt;= 50</code> it draws <code>n</code> from a Poisson distribution with mean <code>mu</code>. It   then adds <code>n</code> frequencies from the table, each placed uniformly within its   frequency bin.</li> <li>For <code>mu &gt; 50</code> it draws the sum directly from the Gaussian with variance   <code>mu &lt;f^2&gt;</code> per axis.</li> </ul> <p>It adds <code>lambda f</code> to the tangential part of the direction and restores the unit length along the normal. A frequency that would make the ray evanescent leaves it specular; at grazing incidence that needs spatial periods shorter than the wavelength and does not occur for measured maps.</p> <p><code>PLANES</code> does not tilt the normal for such a profile, so the slope of the profile is not counted twice. The rms slope error (<code>slopeErrorSag</code>, <code>slopeErrorMer</code>) keeps the geometric treatment: an rms slope does not fix the heights, and the heights are what sets <code>sigma_phi</code>.</p> <p>The grating routines <code>PLGRAT</code>, <code>SPGRAT</code> and <code>TOGRAT</code> call <code>SURFSCAT_APPLY_GRATING</code> after <code>REFRAC</code>, with the sine of the incoming grazing angle taken before the diffraction. It uses the mean of the two sines in <code>mu</code>. <code>SURFSCAT_APPLY</code> is its mirror form, both angles equal.</p> <p>The settings are <code>MPARAM(IM,86)</code> (Profile Treatment) and <code>MPARAM(IM,87)</code> (Edge Diffraction).</p>"},{"location":"SurfaceScattering.html#4-edge-diffraction","title":"4. Edge diffraction","text":""},{"location":"SurfaceScattering.html#41-what-is-implemented","title":"4.1 What is implemented","text":"<p>With Edge Diffraction on, every reflected ray receives a Fraunhofer deflection for the aperture the reflected beam sees. In the plane of incidence that is a slit of width <code>L sin(theta)</code>, with <code>L</code> the mirror length and <code>theta</code> the local grazing angle; across it, a slit of the mirror width <code>W</code>. The deflection is drawn from <code>sinc^2</code> by the same routine RAY-UI uses at slits (<code>FRAUN_DIFFRACTION</code>). On a grating the aperture in the dispersion plane is <code>L sin(beta)</code>, with <code>beta</code> the grazing angle of the diffracted order (section 2.4).</p>"},{"location":"SurfaceScattering.html#42-when-it-is-right-and-when-not","title":"4.2 When it is right, and when not","text":"<p>For a uniformly illuminated, overfilled aperture the ensemble of deflected rays is the Fraunhofer pattern of the aperture. Convolved with the geometric image, it is the partially coherent image. That is the case in which a ray tracer misses the diffraction limit, and in it the setting is exact.</p> <p>It has three limits:</p> <ul> <li>Underfilled mirrors. The diffraction of a beam's own finite size is   already in the rays, through the radiation size and divergence of the source.   A mirror that does not clip the beam adds nothing, but the setting still   deflects every ray for the full aperture. QMAP M6 is filled to 175 of its   200 mm at 400 eV and to 117 mm at 1000 eV. There the setting broadens the   horizontal focus, in FWHM / EE80, by 22 / 38 % and 10 / 17 %, while SRW   agrees with the trace without it at 1000 eV (section 6.2). Switch the setting   on only for mirrors that clip the beam.</li> <li>Mirrors as long as their image distance. Each ray is deflected where it   leaves the mirror and then travels its own distance to the image; for M6,   with <code>L</code> = <code>q</code> = 200 mm, between 100 and 300 mm. The pattern at the image is   therefore a mixture of <code>sinc^2</code> patterns scaled by these distances. The   traced rms broadening in the plane of incidence matches that of a single   <code>sinc^2</code> at 200 mm within 6 % at 50 to 200 eV. The enclosed energies come   out right, EE80 and EE90 within 2 and 5 % of SRW at 50 eV. The rays from the near end, however,   sharpen the peak, and the FWHM of the focus is too small, by 28 % at 50 eV.   A mixture computed from the <code>sinc^2</code> alone reproduces this: FWHM 262 nm   instead of 315 nm for a single distance of 200 mm, but EE90 571 instead of   509 nm. For the usual <code>L &lt;&lt; q</code> all rays travel nearly the same distance and   the effect vanishes.</li> <li>Mirrors strongly curved across the beam. The deflection is applied in the   local plane of incidence. Where the beam overfills a mirror whose sagittal   radius is comparable to its width, that plane tilts across the mirror and part   of the in-plane deflection appears across it. M6 has sagittal radii of 18 to   30 mm on a 50 mm width. At 50 eV its vertical focus broadens by 151 nm rms,   where the 50 mm width alone gives 71 nm. The excess shrinks with the sagittal   footprint and is gone at 400 eV.</li> </ul>"},{"location":"SurfaceScattering.html#43-why-not-a-deflection-that-depends-on-the-distance-to-the-edge","title":"4.3 Why not a deflection that depends on the distance to the edge","text":"<p>Monte Carlo codes sometimes deflect a ray according to its distance <code>d</code> from the nearest edge, by a Gaussian of width about <code>lambda / (4 pi d)</code>. This is the \"Heisenberg uncertainty principle\" method of Freniere, Gregory and Hassler [8]. It would leave underfilled mirrors alone. For a uniformly illuminated slit, however, it cannot give the Fraunhofer pattern. Once its FWHM is matched, 80 % and 90 % of the power lie within 1.7 and 3.0 <code>lambda / D</code> instead of 1.03 and 1.46.</p> <p>The reason is that the exact ray description of an aperture, its Wigner function [9], is <code>sin(2 k theta d) / (pi theta)</code>. That has the <code>lambda / d</code> width of the uncertainty argument, but it takes negative values. Averaged over the aperture it gives <code>sinc^2</code> exactly. No positive kernel can do the same, because the zeros of <code>sinc^2</code> are interference. A position-independent Fraunhofer deflection is exact where edge diffraction matters, which is why it is used here. The complete treatment propagates a wavefront through the element, as the HYBRID method of SHADOW does [10].</p>"},{"location":"SurfaceScattering.html#5-how-to-use-it","title":"5. How to use it","text":""},{"location":"SurfaceScattering.html#51-in-the-parameter-panel","title":"5.1 In the parameter panel","text":"<p>For a mirror or grating with a measured or modelled profile:</p> <ol> <li>Slope Error: <code>Yes</code>.</li> <li>Surface Profile: <code>Profile-File</code>, and Filepath to the profile.</li> <li>Profile Treatment: <code>wave-optical (PSD scattering)</code>.</li> <li>Edge Diffraction (Fraunhofer): <code>on</code> if the mirror clips the beam,    otherwise <code>off</code>.</li> </ol> <p>Profile Treatment is enabled only for a profile file. The thermal bump and the cylindrical bowing are smooth figure shapes and stay geometric. Edge Diffraction is independent of the surface-error settings.</p> <p>The profile file is the format RAY-UI already reads. The first line holds the numbers of points across (x) and along (z) the mirror, then the spacings in mm; then, for each x, a line of the z values. Heights are in nm for the <code>S</code> and <code>Z</code> kinds and in mm for <code>A</code> (ANSYS). At most 251 x 2048 points are used; the mirror size is taken from the profile.</p>"},{"location":"SurfaceScattering.html#52-in-an-rml-file","title":"5.2 In an RML file","text":"<pre><code>&lt;param id=\"slopeError\" comment=\"Yes\" enabled=\"T\"&gt;0&lt;/param&gt;\n&lt;param id=\"profileKind\" comment=\"Profile-File\" enabled=\"T\"&gt;0&lt;/param&gt;\n&lt;param id=\"profileFile\" enabled=\"T\"&gt;/path/to/s_mirror_profile.dat&lt;/param&gt;\n&lt;param id=\"surfaceErrorTreatment\" comment=\"wave-optical (PSD scattering)\" enabled=\"T\"&gt;1&lt;/param&gt;\n&lt;param id=\"edgeDiffraction\" comment=\"on\" enabled=\"T\"&gt;1&lt;/param&gt;\n</code></pre> <p>Both ids can be scanned in a Looper preset like any other parameter. That is the easiest way to compare the two treatments on one beamline.</p>"},{"location":"SurfaceScattering.html#53-reading-the-result","title":"5.3 Reading the result","text":"<ul> <li>Use FWHM and encircled energy, not RMS. The rms width barely changes   between the treatments (section 2.2). The halo carries the same angular   variance as the geometric blur, only in few rays.</li> <li>Enough rays for the halo. At 1000 eV about 3 % of the rays reflected by   QMAP M6 are scattered, and they spread over a few micrometres around a   40 nm core. Halo shapes need correspondingly many rays; FWHM and encircled   energies converge like any other trace.</li> <li>Coating roughness. The N\u00e9vot\u2013Croce roughness of a coating [11] lowers the   reflectivity for microroughness below the resolution of a map. It is a   separate input; do not enter the roughness a profile already contains as   coating roughness as well.</li> </ul>"},{"location":"SurfaceScattering.html#6-validation","title":"6. Validation","text":""},{"location":"SurfaceScattering.html#61-the-statistics-of-the-implementation","title":"6.1 The statistics of the implementation","text":"<p><code>test/unit/surfacescattering/test_surfscat.f90</code> drives <code>SURFSCAT_SETUP</code> and <code>SURFSCAT_APPLY</code> directly. It uses a periodic, Gaussian-correlated profile of 1024 x 128 points with 0.25 nm rms, 4\u00b0 grazing incidence and 400 000 rays per case. One run gave:</p> check result expected <code>sigma_h</code> of the table 0.25000 nm 0.25000 nm specular fraction, <code>mu</code> = 0.1 0.90467 <code>exp(-0.1)</code> = 0.90484 halo rms of the scattered rays, <code>mu</code> = 0.1 3.242e-7 rad 3.244e-7 rad, from the PSD rms deflection in plane, <code>mu</code> = 0.1, 30, 400 1.0009e-7, 1.0007e-7, 0.9999e-7 rad <code>2 m_z</code> = 1.0008e-7 rad rms deflection across, <code>mu</code> = 0.1, 30, 400 1.785e-8, 1.786e-8, 1.781e-8 rad <code>2 sin(theta) m_x</code> = 1.783e-8 rad edge diffraction FWHM, in plane / across 1.025 / 1.025 1, i.e. <code>0.886 lambda / D</code> grating, <code>alpha</code> = 0.87\u00b0, <code>beta</code> = 1.95\u00b0: specular fraction, <code>mu</code> = 0.3 0.7428; six further runs 0.7393 to 0.7413 <code>exp(-0.3)</code> = 0.7408 grating: edge diffraction FWHM with <code>L sin(beta)</code> 1.025 1 <p>The FWHM is read from a histogram with a bin of 0.025 of the expected width. The rms deflection is the same at every phase error, as section 2.2 states, and equals twice the slope the table's PSD implies. From finite differences of the map the slope comes out about 2.5 % different, because of the window and the discretisation; that is a property of the slope estimate, not of the scattering. The test exits with status 1 if a check leaves its tolerance of about four standard errors; eight repeated runs passed.</p>"},{"location":"SurfaceScattering.html#62-against-srw","title":"6.2 Against SRW","text":"<p>The QMAP beamline of section 1 was traced with ideal mirrors up to M4 and the synthetic directional-PSD maps of M5 and M6 as profile files: a Gaussian undulator source with a 0.07 % band, 10^6 rays per point. Four variants differ only in the two settings on M5 and M6. The Focus and exit-slit rays were reduced with the same metrics as the SRW intensities: - the FWHM around the highest peak, from a histogram with a bin of 1/20 of the   50 % width, smoothed 1-2-1; - the narrowest intervals holding 80 and 90 % of the power, EE80 and EE90.</p> <p>The SRW reference at 400 and 1000 eV is the 0.07 % band with 328 macro-electrons. At 50 to 150 eV it is monochromatic with 64 macro-electrons. Those low-energy runs predate a phase fix in SRW's focal transforms that moved the illumination of M6, so they are provisional, and only the horizontal axis is compared there.</p> <p>Regression. With both settings at their defaults, this branch traces bit-identically to dev: the same Focus rays, checksum included, at 50 eV with 400 000 rays.</p> <p>Focus, horizontal (FWHM / EE80 / EE90 in nm; M6 footprint from RAY-UI):</p> E [eV] M6 lit SRW geometric wave-optical wave-optical + edge on M6 50 200 mm 321.8 / 390.9 / 634.2 148.5 / 213.1 / 301.1 107.1 / 163.9 / 240.8 230.8 / 399.6 / 605.5 100 200 mm 166.0 / 413.2 / 1331 139.1 / 186.8 / 254.3 85.5 / 111.7 / 154.4 134.7 / 229.5 / 350.6 150 200 mm 115.0 / 192.8 / 351.2 139.0 / 177.5 / 240.3 69.4 / 93.4 / 126.8 105.3 / 166.1 / 248.1 200 200 mm \u2013 141.3 / 179.9 / 240.8 66.7 / 83.2 / 112.8 89.1 / 134.2 / 199.9 400 175 mm 59.7 / 74.1 / 110.8 147.4 / 180.8 / 239.1 50.1 / 63.4 / 84.9 61.2 / 87.3 / 124.3 800 136 mm \u2013 152.6 / 186.4 / 243.2 42.5 / 51.4 / 68.1 48.2 / 62.0 / 85.9 1000 117 mm 42.0 / 49.3 / 66.7 163.5 / 190.3 / 249.3 40.8 / 49.7 / 66.2 44.7 / 58.2 / 80.0 <ul> <li>Underfilled M6, high energy. The wave-optical treatment alone   reproduces SRW: at 1000 eV within -3, +1 and -1 %, where the geometric focus   is 3.9 times too wide. Edge diffraction would add 10 to 20 % here, see   section 4.2.</li> <li>Overfilled M6, low energy. The wave-optical treatment alone is too   narrow, because it removes the slope blur and adds no diffraction. With edge   diffraction on M6, EE80 and EE90 lie within +2 and -5 % of SRW at 50 eV,   against -45 and -53 % for the geometric trace. The FWHM stays 28 % small, as   section 4.2 explains. At 150 eV the three widths lie -8, -14 and -29 % from   SRW. At 100 eV the provisional SRW run has tails to 1.3 um in EE90, so only   its FWHM, 166 against 135 nm, is compared.</li> <li>The geometric agreement near 100 to 150 eV is a coincidence. A slope blur   that is too large stands in for the missing diffraction. The FWHM happens to   fit, but the enclosed energies do not, for example EE90 at 150 eV: 240 nm   against 351 nm in SRW.</li> <li>Partly filled M6, 400 eV. SRW lies between the two settings: -16, -14   and -23 % without edge diffraction, +3, +18 and +12 % with it. A deflection   for the full aperture cannot represent a mirror that clips only the tails of   the beam.</li> <li>Edge diffraction on M5 as well widens the focus by a further 0 to 9 %.   M5 is filled to 346 of its 500 mm at 50 eV and 243 mm at 100 eV, so it does   not clip, and the setting stays off there.</li> </ul> <p>Focus, vertical (0.07 % band in both codes, FWHM / EE80 / EE90 in nm):</p> E [eV] SRW geometric wave-optical 400 86.7 / 72.5 / 83.7 94.4 / 104.2 / 133.6 89.7 / 81.3 / 99.7 1000 94.8 / 80.1 / 90.6 105.3 / 107.2 / 134.4 104.5 / 89.7 / 107.9 <p>The wave-optical treatment reduces the excess in EE80 from 34 to 44 % to 12 % and in EE90 from 48 to 60 % to 19 %. The rest was not traced further here.</p> <p>Exit slit. The two settings act only on M5 and M6. The exit slit is identical in all variants: 14.9 to 15.0 um vertically against 15.1 um in SRW. Horizontally, RAY-UI is already wider at the slit at low energy: 25.8 against 17.1 um at 50 eV, 14.0 against 10.0 um at 150 eV, and 5.9 against 6.1 um at 1000 eV. That comes from the source model, not from the mirrors, and it enters the horizontal focus from 50 to 150 eV.</p> <p>Statistics. 8 000 of the 10^6 rays reach the focus at 50 eV and 105 000 at 1000 eV. Repeated traces of the same variant are identical, so the scatter between neighbouring energies is the only measure of the ray noise. At 50 eV it is a few percent in the enclosed energies.</p> <p>Recommendation. - Use the wave-optical treatment for every mirror with a measured or modelled   profile. - Switch edge diffraction on only for mirrors whose footprint fills their   length, as M6 up to 200 eV. - Where a mirror clips only the tails of the beam, the traces with and without   edge diffraction bracket the result.</p>"},{"location":"SurfaceScattering.html#63-gratings-in-a-trace","title":"6.3 Gratings in a trace","text":"<p>The RAY-UI test beamline <code>test/scripting/GratingEfficiencyTest.rml</code> was changed to a collimated, monochromatic 400 eV beam that overfills its 200 x 50 mm plane grating: 150 l/mm, grazing incidence 0.867\u00b0 and exit 1.950\u00b0. Without surface errors every diffracted ray leaves in the same direction. The fraction of rays still in that direction, and the angular spread, can therefore be measured directly at the image plane, with 10^6 rays traced and 95 000 arriving.</p> check trace expected specular fraction, wave-optical, synthetic profile with <code>sigma_h</code> = 7 nm 0.6129 \u00b1 0.0016 <code>exp(-mu)</code> = 0.6147; with <code>beta</code> alone, as on a mirror, 0.393 angular FWHM in the dispersion plane, edge diffraction on 1.000 1, i.e. <code>0.886 lambda / (L sin(beta))</code> angular FWHM across it 1.000 1, i.e. <code>0.886 lambda / W</code> both settings off no spread none <p>The widths come from a histogram with a bin of 1/40 of the expected width.</p> <p>With both settings at their defaults the three grating routines trace bit-identically to dev: - QMAP, with its plane grating, at 50 eV; - <code>RAYpy/rml/beamline.rml</code>, with a spherical grating; - the same beamline with its grating loaded as a toroidal grating.</p> <p>With edge diffraction on, the same beamlines keep their ray counts and change their directions. The mirror traces of section 6.2 are also bit-identical before and after the grating routine was split off.</p> <p>The gratings have not yet been compared with a wave-optics code.</p>"},{"location":"SurfaceScattering.html#7-assumptions-and-limits","title":"7. Assumptions and limits","text":"<ul> <li>Statistics, not the local patch. The table describes the whole map as a   stationary Gaussian process. A ray scatters according to the statistics of the   surface, not according to the particular patch it hits. A single deterministic   feature, such as a bump or a bend, is averaged into the halo. Deterministic   figure shapes belong in the geometric treatment.</li> <li>Kirchhoff approximation. The treatment is scalar, with small slopes and   without shadowing or multiple scattering on the surface. Grazing-incidence   mirrors satisfy all of these.</li> <li>Reflectivity. A scattered ray keeps the reflectivity computed for the   specular direction, and on a grating the efficiency of its order.</li> <li>Grating mounts. The edge deflection uses the apertures <code>L sin(beta)</code> and   <code>W</code> about the diffracted ray. That is exact when the ray lies in the plane of   the grating length, as in the classical in-plane mount. In off-plane and   conical mounts (<code>REFRAC2D</code>) the projected aperture is a parallelogram, and   the separable deflection is an approximation. The reflection zone plate   (<code>RZP</code>), whose two-step diffraction differs from a grating's, is not included.</li> <li>Resolution of the map. Spatial periods longer than the map or shorter than   twice its spacing are missing from the PSD.</li> <li>Edge diffraction. See section 4.2.</li> <li>Threads and reproducibility. The tables are module state and the draws   use the Fortran <code>random_number</code> generator. Both are safe because RAY-UI   traces optical elements single-threaded: no mirror sets <code>isThreadSafe</code>, so   the OpenMP ray loop of the renderer never runs over them. A mirror marked   thread-safe would need per-thread generators. A given beamline, ray number   and build traces to identical rays on every run.</li> </ul>"},{"location":"SurfaceScattering.html#references","title":"References","text":"<ol> <li>P. Beckmann, A. Spizzichino, The Scattering of Electromagnetic Waves from    Rough Surfaces, Pergamon, Oxford (1963).</li> <li>J. E. Harvey, Light-scattering characteristics of optical surfaces, PhD    thesis, University of Arizona (1976).</li> <li>J. E. Harvey, A. Krywonos, C. L. Vernold, \"Modified Beckmann\u2013Kirchhoff    scattering model for rough surfaces with large incident and scattering    angles\", Opt. Eng. 46, 078002 (2007).</li> <li>S. K. Sinha, E. B. Sirota, S. Garoff, H. B. Stanley, \"X-ray and neutron    scattering from rough surfaces\", Phys. Rev. B 38, 2297 (1988).</li> <li>H. E. Bennett, J. O. Porteus, \"Relation between surface roughness and    specular reflectance at normal incidence\", J. Opt. Soc. Am. 51, 123    (1961).</li> <li>E. L. Church, H. A. Jenkinson, J. M. Zavada, \"Relationship between surface    scattering and microtopographic features\", Opt. Eng. 18, 125 (1979).</li> <li>J. C. Stover, Optical Scattering: Measurement and Analysis, 3rd ed., SPIE    Press, Bellingham (2012).</li> <li>E. R. Freniere, G. G. Gregory, R. A. Hassler, \"Edge diffraction in Monte    Carlo ray tracing\", Proc. SPIE 3780, Optical Design and Analysis Software    (1999).</li> <li>M. J. Bastiaans, \"The Wigner distribution function applied to optical    signals and systems\", Opt. Commun. 25, 26 (1978).</li> <li>X. Shi, R. Reininger, M. Sanchez del Rio, L. Assoufid, \"A hybrid method for     X-ray optics simulation: combining geometric ray-tracing and wavefront     propagation\", J. Synchrotron Rad. 21, 669 (2014).</li> <li>L. N\u00e9vot, P. Croce, \"Caract\u00e9risation des surfaces par r\u00e9flexion rasante de     rayons X\", Rev. Phys. Appl. 15, 761 (1980).</li> <li>M. Born, E. Wolf, Principles of Optics, 7th ed., Cambridge University     Press (1999): the Strehl ratio and the Mar\u00e9chal criterion (section 9.1),     Fraunhofer diffraction at a slit (section 8.5).</li> </ol>"}]}